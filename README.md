
  # Tutor Matching Platform Prototype

  This is a code bundle for Tutor Matching Platform Prototype. The original project is available at https://www.figma.com/design/O1bls80hLe7yx5javz0WD6/Tutor-Matching-Platform-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  